apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deploy
  namespace: test-flux  
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-api
  template:
    metadata:
      labels:
        app: hello-api
    spec:
      containers:
        - name: hello-api
          image: nginx:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
